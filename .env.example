# Copy this file to .env.local and fill in the actual values
# .env.local should be added to .gitignore

# Production API Key (keep secret!)
PROD_API_KEY=your_actual_prod_api_key_here

# Firebase Project IDs (optional, if using separate Firebase projects)
DEV_FIREBASE_PROJECT_ID=your-dev-firebase-project
PROD_FIREBASE_PROJECT_ID=your-prod-firebase-project
